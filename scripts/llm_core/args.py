# -*- coding: utf-8 -*-
"""
LLM Core Arguments Module.

This module will handle the parsing of command-line arguments for LLM tasks.
It will provide a base argument parser that can be extended by individual task scripts.
Common flags such as --issue, --ac, --observation, --two-stage, --select-context,
--yes, --only-meta, --only-prompt, --web-search, --exclude-context will be defined here.
The parse_arguments function from llm_interact.py will be moved and adapted here.
"""
# Argument parsing logic will be implemented here in subsequent refactoring steps.