[pytest]
pythonpath = .
markers = 
    live: mark test as live to run only when --live is provided and API key is set
    integration: mark test as integration test
    stage_test: mark test as related to generate_context.py stage selection